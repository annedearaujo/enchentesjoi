<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReportFlood" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:textColor">@color/textColor</item>
        <item name="android:background">@color/backgroundColor</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.ReportFlood" parent="Base.Theme.ReportFlood" ></style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light">
        <!-- Personalizações específicas para os menus de sobreposição -->
        <item name="android:colorBackground">@color/overlayBackgroundColor</item>
        <item name="android:textColor">@color/overlayTextColor</item>
        <!-- Adicione outras personalizações conforme necessário -->
    </style>

</resources>